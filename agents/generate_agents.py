"""Agent generation — stub for Area 1 implementation."""

from agents.feature_subset_agent import FeatureSubsetAgent


def generate_feature_subset_agents(
    feature_names: list[str],
    max_agents: int = 100,
    seed: int = 42,
) -> list[FeatureSubsetAgent]:
    """Generate feature-subset agents. Stub — to be implemented in Area 1."""
    raise NotImplementedError("Agent generation not yet implemented")
